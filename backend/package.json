{
    "name": "backend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
      "start": "node index.js",
      "build:frontend": "cd ../frontend && npm install && npm run build && cp -r build ../backend/public",
      "deploy": "npm run build:frontend && node index.js",
      "dev": "nodemon index.js"
    },
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "pg": "^8.11.3",
      "pg-hstore": "^2.3.4",
      "sequelize": "^6.32.1"
    },
    "devDependencies": {
      "nodemon": "^3.0.2"
    }
  }